# Copyright (c) 2020 Ryan Cross
# SPDX-License-Identifier: MIT

# Uncomment the following line to enable the Sofle OLED Display
CONFIG_ZMK_DISPLAY=y

# Uncomment these two lines to add support for encoders
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

# enable deep sleep to save power
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000

# nice!view is a persistent Sharp Memory LCD with no backlight, so it does not
# visibly go dark on blank/idle - the last frame stays on the glass.
CONFIG_ZMK_DISPLAY_BLANK_ON_IDLE=y
CONFIG_ZMK_IDLE_TIMEOUT=15000

# BLE TX power left at default (0 dBm) to maximize battery life.
# Re-enable the line below (+8 dBm) if BLE dropouts/lag return.
# CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Force the connection to be more responsive (reduces retry lag)
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=5
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=5

# Improve Bluetooth stability
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y

# Uncomment the line below to add rgb underglow / backlight support
# CONFIG_ZMK_RGB_UNDERGLOW=y

# Uncomment the line below to disable external power toggling by the underglow.
# By default toggling the underglow on and off also toggles external power
# on and off. This also causes the display to turn off.
# CONFIG_ZMK_RGB_UNDERGLOW_EXT_POWER=n
