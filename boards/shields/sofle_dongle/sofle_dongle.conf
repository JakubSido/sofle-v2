CONFIG_ZMK_SPLIT=y
CONFIG_ZMK_SPLIT_BLE=y

# Dongle outputs over USB, not BLE HID
CONFIG_ZMK_USB=y
CONFIG_ZMK_BLE=y

# Dongle has no display, encoders, or matrix — override the globals from sofle.conf
CONFIG_ZMK_DISPLAY=n
CONFIG_EC11=n
